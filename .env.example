# Search Provider: "scraper" (recommended) or "google-cse"
# - scraper: Uses Puppeteer to scrape Google (same results as browser)
# - google-cse: Uses Google Custom Search API (may return different results)
SEARCH_PROVIDER=scraper

# Google Custom Search API credentials (only needed if SEARCH_PROVIDER=google-cse)
# Get these from:
# - API Key: https://console.cloud.google.com/apis/credentials
# - CSE ID: https://programmablesearchengine.google.com/controlpanel/all
GOOGLE_API_KEY=your_api_key_here
GOOGLE_CSE_ID=your_search_engine_id_here
